# Eng session 2026-09-05-check-runtime-homes

- Created: 2026-09-05
- Work type: W1
- Goal: Finish check-runtime homes (2.35.0)

## Log

- N1: `check_street_bind` applies `_apply_y1_policy` (bands/responses from `StreetY1Policy`). Deleted `_check_gated_y1_band`. `_fy1_year_row` shared pick; numeric fallback stays on `fy1_street_revenue`. Stacking takes `fail_band`. Injected non-5% band test.
- N2: Domain bodies left `gates.py`. Homes: `valuation_hygiene` (MoS), `decision` (scenario/stress/LQ), `consumption` (FDD/MC hooks), `annuals` (year-dive complete), `provenance` (LLM identity), `phase_status` (disk/handoff/phase0), `isolation.py` (Agent 4). `gates` re-exports. `_report_rows(..., exact=)`.
- N3: Exclusive `session_core` / `session_full` via `run_catalog`. Same-function rows gained tags; wave2/3/4 `--full` bundles are new rows (not destock-only phase wrappers). CLI-only still in `check_session` (`record()` for identity/MC/brief/phase_status/isolation/library/spawn/meta/FDD content/risk_bridge/audit/handoffs).
- Why: use 2.34.0 catalog + policy interfaces; no `session_checks` / `phase_coverage` drawers.
- `harness/VERSION` 2.35.0.
