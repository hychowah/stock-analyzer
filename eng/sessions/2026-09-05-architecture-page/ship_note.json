{
  "summary": "Add GET /architecture: live working-tree ARCHITECTURE.md as sanitized HTML, with header nav and GitHub-style heading ids in the shared markdown renderer.",
  "verify_commands_run": [
    "python -m pytest apps/analysis_web/tests scripts/tests/test_architecture_doc.py -q",
    "python scripts/eng_verify.py --quick",
    "smoke GET http://127.0.0.1:18765/architecture (nav, heading ids, tables)"
  ],
  "follow_ups": [
    "Restart the usual :8765 UI to pick up the new route (auto-restart is git HEAD, not file watch).",
    "Optional later: mermaid.js for the flowchart fences."
  ],
  "risks": [
    "Heading ids are now on all rendered markdown (reports too); existing tests that required a bare <h1> tag were updated."
  ]
}
