{
  "harness_version": "2.36.0",
  "harness_spec": "v2",
  "notes": "2.36.0: check catalog is the only when-table (atomic named rows; check_session is argparse + run_catalog; no record()). StreetY1Policy is the only Street-era statute; hygiene is its own catalog row. No Mode A statute change. 2.35.0: Street bind applies policy; knowledge homes; exclusive session_full. 2.34.0: check catalog + check_core."
}
