# Eng session 2026-09-06-ui-a11y-phone

- Created: 2026-09-06T10:37:38Z
- Work type: W4
- Goal: Keyboardable disclose chrome, AA contrast leftovers, raise stack-table breakpoint to 1100px, phone leftovers.

## Log

- 2026-09-06T10:37:38Z scaffolded
- 2026-09-07 orient: on `ui-ux-compose` at `140061b` (includes Wave 4c product `6449cfd` + verifier; Wave 3 `e581694`; Wave 2 `e3f9755`; Wave 1 `d21af66`; verifier `457aefb`). Read `eng/AGENTS.md`, parent PLAN.md § Session 5 + file-ownership + How a later agent starts, `AGENT_BRIEF.md`, `handoffs/sdr-wave4b-a11y-phone.md`. Predecessors `eng/sessions/2026-09-06-ui-nav-ia/` and `eng/sessions/2026-09-06-ui-decision-blotter/` (skip link and `.decision` exist). SDR mixed/reshape: one 1100px contract for every `app.css` 800px query (including `.chart-stage` query width; height values stay 4a). Do not clip `.stack-table thead`. List replacement = live node outside the swap + `runs-table-updated` (not `catalog-changed`). Drop harness.css 900px from this session. No sticky site header. No second card markup. Stale `:8765` git unreadable; this-tree UI booted on `:8776`.
- 2026-09-07 implemented Wave 5 (implementer; `passes` left for verifier).
- **breakpoint-1100:** One named 1100px contract comment; every `max-width: 800px` in `app.css` is now 1100px (Menu, stack-table, grid2, chart-stage query, perf bars, forms). README “Below 1100px”. Chart-stage HEIGHT still 220px in that query (Wave 4a).
- **disclose-focus:** Class-level `display: none` on `.disclose` / `.disclose-btn` at `min-width: 1101px` (Menu + Lab + Filters). Phone `:focus-visible` ring on `.disclose-btn`.
- **contrast:** Night `--btn-bg: #2563eb` in both dark token tables. `.quote-kind` and `.chart-grid text` use `--muted`. `--faint` left for non-text.
- **thead:** Keep `display: none` (not clipped). After swap, `aria-label` from `data-label` on non-pick `td`s. Empty pick `th` is `aria-label="Select for compare"`.
- **live-focus:** `#runs-status` (`aria-live="polite"`) outside `#runs-results`. Count `aria-live` stripped from the partial. After swap: copy `data-runs-status`, restore focus to `a.sort.is-active` or ticker, `dispatchEvent("runs-table-updated")`. Fetch failure writes the status node + `#runs-flash`; stale table stays. `compares.js` listens to `runs-table-updated` (not `catalog-changed`). `#compare-hint` is a separate live region; hint is ticker + `data-session-key`. Compare pick 44px hit; `#compare-bar.is-on` sticks only while a pick is on. 16px inputs; ticker `flex: 1 1 100%`; overflow-x on report/matrix tables.
- **figures:** Architecture frame `min-height: 16rem; height: min(50vh, 22rem)` inside the 1100px query. `controlIconsEnabled: true`. `harness.css` untouched.
- Did not rewrite column order, `.decision` / `.below-bear`, harness.css, `price_chart.js`, `quotes.js`, football PNG, or nav grouping. No `harness/VERSION` bump (W4).
- Verify (implementer, not flipping `passes`): `pytest apps/analysis_web/tests` 220 passed; `eng_verify.py` PASS (811, was 803). Live this-tree `:8776` SHA `140061b…`: `#runs-status` outside `#runs-results`; fragment has `data-runs-status` and no `aria-live`; CSS has no `max-width: 800px`. Edge smoke: desktop first Tab is skip-link (not `#nav-open`); ~844px (`innerWidth` 820) stacked cards (`thead` none, `tr` block); sort links not visible tab stops. Did not rewrite `base.html`, `quotes.js`, `price_chart.js`, harness.css, football PNG, nav grouping, or `harness/VERSION`.
- `ARCHITECTURE.md` § The website (`/architecture` zoom controls). No `harness/VERSION` bump (W4).
- Product committed as `6560a74` after /smart-commit (implementer work). Stay on `ui-ux-compose`.
- 2026-09-07 verifier (skeptical): pytest analysis_web 220 passed; eng_verify PASS 811. progress.md names predecessors ui-nav-ia and ui-decision-blotter. app.css has no max-width: 800px; min-width 1101px display none on .disclose; both dark --btn-bg #2563eb; quote-kind and chart-grid text use --muted; stack-table thead is display:none not clip. runs.js dispatches runs-table-updated; compares.js listens, no catalog-changed. mermaid_boot controlIconsEnabled true; harness.css still 900px. Forbidden files vs 140061b untouched (base.html, quotes.js, price_chart.js, harness.css, harness/VERSION); .decision / .below-bear not in the app.css diff. Live :8778 SHA 6560a74: Edge first Tab is skip-link not #nav-open; ~844px innerWidth 820 stacked cards; sort links not visible tab stops. TestClient: / runs-status before results; fragment data-runs-status and no aria-live; /architecture On-figure controls. Flipped feature_list passes; wrote ship_note.json. Did not git commit as part of verify.
