{
  "harness_version": "2.35.0",
  "harness_spec": "v2",
  "notes": "2.35.0: Street bind applies StreetY1Policy; remaining gates domain law in knowledge homes; --full dispatches run_catalog(session_full) instead of the old domain step list; CLI-only identity/FDD/risk_bridge/audit/handoffs still run via record(). No Mode A statute change. 2.34.0: check catalog + check_core."
}
