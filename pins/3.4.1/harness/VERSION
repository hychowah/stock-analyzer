{
  "harness_version": "3.4.1",
  "harness_spec": "v3",
  "notes": "3.4.1: classification.json is the only identity; 3P PASS is the lock; Agent 5 compute-only (story_input_bind + buyer_dials); evidence_hooks cover each id'd finding. 3.4.0: Engine classification at orch; phase 1e story writer + 3P critic. 3.3.0: Tier-1 Damodaran integration — router adds the buyer question and contingent_claim/asset_based primaries with ref-recipe pointers Agent 5 must open; narrative_bind needs life_cycle_stage/stage_fit, buyer, market_contest, option_screens, distressed_call; every model answers truncation (p=0 + why_not_material, or p>0 weighted base); wacc_buildup needs erp_method/beta_method/discount_currency/cash_flow_currency (market-neutral); per_share_bridge required; Windows UTF-8 file reads. 3.2.0: Narrative substance gate (narrative_bind needs story.paragraph + a map whose keys name real model inputs + non-empty 3p{possible,plausible,probable}; possible strands keep revenue_in_dcf=0); truncation with p>0 must write the probability-weighted base (no material flag bypass); pricing.used_as is a closed price-only role set; a v3 session with a valuation_model and no parseable harness_version FAILs (root of trust); risk_bridge accepts region scenarios; sector module engine leaks fixed. 3.1.0: Value vs price + engine from the asset (required iv_playbook, required terminal_consistency, sector modules off engine choice, REIT keeps WACC). 3.0.1: Street FY+1 is the default Y1 forecast (street_baseline); independent_y1 needs a named gate; story_fail requires will_own=false. 3.0.0: Ready-set DAG + Damodaran lock-then-compute (independent Y1 default)."
}
