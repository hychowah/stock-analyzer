# Report prose quality (Agents 7 and 11)

Reader-facing reports. Short sentences. Everyday words. One idea per sentence. Keep every fact, number, and caveat. Source paths are citations, not the sentence.

Duration uses the **product English** map only (`packages.kd_research.decision.DURATION_LABELS`): Do not initiate / Hold / Initiate / Add / Trim / Sell / Short / Too hard. Do not invent a third duration phrase the gate cannot recognize. The machine token may appear once as a source line.

## GOOD

Do not initiate. The freeze price is $654.50.

## Unstressed vs stressed

Unstressed 478.86. Stressed 239.43 (advertisers cutting budgets the way they did in 2022). Expected loss 14.0 percent. Size cap 0.00.

## BAD

**`duration.action = pass`** — Clean-sheet initiate fails the price test (`stress_bind.material=true`). Cheap_claim.class = franchise_mos.

## Forecast path

Copy year-by-year dollars from `explicit_forecast` / `data/compute/valuation_result.json`. Heading `## Forecast path`. Do not invent operating profit from a margin.
