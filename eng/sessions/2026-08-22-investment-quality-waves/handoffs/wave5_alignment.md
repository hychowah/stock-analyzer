# Wave 5 alignment — mid-cycle construction

Verdict: **ALIGN WITH EDITS**.

Legal W1; VERSION 2.13.0 same change set; extend `roic_identity.py`; keep `<2.13.0` fixtures green; TTC&lt;WACC stays non-FAIL; do not block `initiate` on non-franchise cheap.

Must-changes: unify license test (`above_wacc` **or** `franchise_mos` only with `ttc_cycle`/`multi_year_avg` **and** ≥2-year window); specify `years_used`; Pair 8 GOOD machine-true (peak 18% not in mid_cycle_roic unless windowed); `4-midcycle` grades license not only franchise_mos; tests include `test_roic_identity.py`.
