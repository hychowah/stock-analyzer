{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "earning_power_changelog.schema.json",
  "title": "Earning-power changelog (update mode)",
  "description": "Facts that changed vs a declared prior session. Forbidden: prior fair_value, MoS, WACC, scenario masses as inputs (F14/F16).",
  "type": "object",
  "additionalProperties": true,
  "required": ["facts"],
  "properties": {
    "prior_session_key": { "type": "string" },
    "facts": {
      "type": "object",
      "description": "nopat, invested_capital, share_count, scorecard rows, etc."
    },
    "thesis_delta": { "type": "string" }
  }
}
