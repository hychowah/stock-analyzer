{
  "harness_version": "2.40.0",
  "harness_spec": "v2",
  "notes": "2.40.0: wacc_buildup identity — Kd is current yield or Rf+spread (coupon illegal); pretax Kd >= Rf unless negative_rate_market; trough current_market weights illegal when distressed tripwire fires; matching CAPM is not a quality badge. 2.39.0: catalog listing fields stamped at export/rebuild (quote_symbol from run_manifest only; quote_listing stamp then snapshot then ticker). 2.38.0: phase DAG lives in phase_graph.PhaseNode; derived tables; edges (after overlays) are the gate. 2.37.0: live operator law is current-only (one orch sequence: listing → bind → classify → brief → 0 ∥ 1_parallel; 1d waits on Phase 0; charts after 2.5; finalize copies scaffold stamps). Street/destock version bands live in law_history.md. 2.36.0: check catalog is the only when-table. 2.35.0: Street bind applies policy."
}
